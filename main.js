var appModel = new AppModel();

var appView = new AppView({ model: appModel });

// appModel.get('videos').set('query', 'asmr');
//^^implement the above for something along the lines of a default APP??
