// IS THIS ONE NECESSARY???
// Combine with ListModel into VideoModel, which can then be rendered by PlayerView and ListView, depending on whether it's the feature ???

let ListModel = Backbone.Model.extend({});
