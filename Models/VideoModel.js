//I need to create the video model. This will be used for the main video and for the queued videos.
var VideoModel = Backbone.Model.extend({
  // idAttribute: '_id',
//Set up JSON
  defaults: function () {
    return {
//key value pairs of default JSON values???
    }
  }
});
